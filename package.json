{
  "name": "mc-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd dashboard && npm install && npm run build",
    "start": "concurrently \"node bot/index.js\" \"cd dashboard && npm start\"",
    "dev": "concurrently \"node bot/index.js\" \"cd dashboard && npm run dev\""
  },
  "dependencies": {
    "mineflayer": "^4.23.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "concurrently": "^8.2.2"
  }
}
